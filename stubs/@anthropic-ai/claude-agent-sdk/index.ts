// Stub for @anthropic-ai/claude-agent-sdk — Anthropic Agent SDK types.
// The source only uses `PermissionMode` as a type import from this package.

export type PermissionMode = 'default' | 'acceptEdits' | 'bypassPermissions' | 'plan'
